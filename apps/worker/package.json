{
  "name": "@gather/worker",
  "version": "0.1.0",
  "private": true,
  "license": "AGPL-3.0-only",
  "type": "module",
  "main": "./dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node --enable-source-maps dist/main.js",
    "clean": "rm -rf dist",
    "retention:purge": "node --enable-source-maps dist/cli/retention-purge.js",
    "keys:rotate": "node --enable-source-maps dist/cli/rotate-key.js",
    "audit:anchor": "node --enable-source-maps dist/cli/audit-anchor.js"
  },
  "dependencies": {
    "@gather/core": "workspace:*",
    "@gather/db": "workspace:*",
    "@gather/mail": "workspace:*",
    "@gather/reminders": "workspace:*",
    "@gather/storage": "workspace:*",
    "pg-boss": "^12.30.0"
  }
}
