{
  "name": "gather",
  "version": "0.1.0",
  "private": true,
  "description": "Open-source client document collection. Stop being your clients' alarm clock.",
  "license": "AGPL-3.0-only",
  "type": "module",
  "packageManager": "pnpm@10.15.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @gather/web dev",
    "typecheck": "pnpm -r typecheck",
    "lint": "eslint .",
    "format": "prettier --check .",
    "format:write": "prettier --write .",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "db:generate": "pnpm --filter @gather/db db:generate",
    "db:migrate": "pnpm --filter @gather/db db:migrate",
    "seed:templates": "pnpm --filter @gather/db seed:templates",
    "verify:audit": "pnpm --filter @gather/db verify:audit",
    "docs:sources": "node scripts/template-sources.mjs",
    "docs:sources:check": "node scripts/template-sources.mjs --check"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@next/eslint-plugin-next": "^16.2.12",
    "@playwright/test": "^1.62.1",
    "@types/node": "^26.1.2",
    "eslint": "^10.8.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-react-hooks": "^7.1.1",
    "globals": "^17.8.0",
    "prettier": "^3.9.6",
    "prettier-plugin-tailwindcss": "^0.8.1",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.65.0",
    "vitest": "^4.1.10"
  }
}
